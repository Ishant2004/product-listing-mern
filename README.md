## MERN stack product listing app

### Setup .env file

```shell
MONGO_URI=your_mongo_uri
PORT=5000
```

### Run this app locally

```shell
npm run build
```

### Install NODE_ENV globally

```shell
npm install -g win-node-env
```


### Start the app

```shell
npm run start
```
